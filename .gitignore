.DS_Store
.cursor/

# Local secrets (never commit)
.env
.env.local
.env.*.local

# Local archives / heavy media (repo root only — public/assets copies ship with Pages)
leadagentsstudio-site.zip
SMARTLINE_LEA_DIALOGUE_FINAL.mp4
/bg-video.mp4
/video1.mp4
/video2.mp4

# Wrangler local state
.wrangler/

# Nested repo — do not commit
public/.git/
